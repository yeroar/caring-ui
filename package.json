{
  "dependencies": {
    "husky": "^8.0.2",
    "style-dictionary": "^3.7.1",
    "token-transformer": "^0.0.27"
  },
  "scripts": {
    "build": "npx token-transformer tokens output.json && npx style-dictionary build",
    "scriptname": "npx token-transformer tokens output.json && npx style-dictionary build"
  }
}
